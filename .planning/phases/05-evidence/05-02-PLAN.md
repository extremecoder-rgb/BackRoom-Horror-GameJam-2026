---
phase: 05-evidence
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/game/WinConditions.js
  - src/game/Scoring.js
  - src/game/ExitDoor.js
  - src/game/DeathHandler.js
  - src/game/SpectatorMode.js
  - src/ui/VictoryScreen.js
  - src/ui/GameOverScreen.js
  - src/ui/GuessModal.js
autonomous: true
requirements:
  - WIN-01
  - WIN-02
  - WIN-03
  - WIN-04
  - WIN-05
  - WIN-06
  - WIN-07
  - WIN-08
  - WIN-09
  - WIN-10
  - WIN-11
  - WIN-12
must_haves:
  truths:
    - "Player must collect 2+ evidence types before guess is allowed"
    - "G key opens ghost type guess modal"
    - "Guess modal shows 6 ghost type options (filtered to possible)"
    - "Correct guess unlocks exit door"
    - "All players reaching exit door triggers victory"
    - "Victory screen shows evidence found, time, score"
    - "Ghost catching player during hunt causes death"
    - "Dead players enter spectator mode"
    - "All players dead triggers game over screen"
    - "Scoring awards points for evidence, ID, escape"
    - "Restart option appears on victory/game over"
  artifacts:
    - path: "src/game/WinConditions.js"
      provides: "Win/lose condition checking, guess validation"
      exports: ["WinConditions", "class"]
      min_lines: 80
    - path: "src/game/Scoring.js"
      provides: "Points calculation system"
      exports: ["Scoring", "class"]
      min_lines: 60
    - path: "src/game/ExitDoor.js"
      provides: "Exit door unlock and escape detection"
      exports: ["ExitDoor", "class"]
      min_lines: 50
    - path: "src/game/DeathHandler.js"
      provides: "Player death and state change"
      exports: ["DeathHandler", "class"]
      min_lines: 50
    - path: "src/game/SpectatorMode.js"
      provides: "Camera handling for dead players"
      exports: ["SpectatorMode", "class"]
      min_lines: 50
    - path: "src/ui/VictoryScreen.js"
      provides: "Victory UI with stats and score"
      exports: ["VictoryScreen", "class"]
      min_lines: 60
    - path: "src/ui/GameOverScreen.js"
      provides: "Game over UI with restart option"
      exports: ["GameOverScreen", "class"]
      min_lines: 50
    - path: "src/ui/GuessModal.js"
      provides: "Ghost type selection modal"
      exports: ["GuessModal", "class"]
      min_lines: 60
  key_links:
    - from: "WinConditions.js"
      to: "JournalManager.js"
      via: "hasMinimumEvidence() check"
      pattern: "hasMinimumEvidence"
    - from: "WinConditions.js"
      to: "ExitDoor.js"
      via: "openExit() triggers unlock"
      pattern: "openExit"
    - from: "ExitDoor.js"
      to: "WinConditions.js"
      via: "checkVictory() all escaped"
      pattern: "checkVictory"
    - from: "DeathHandler.js"
      to: "SpectatorMode.js"
      via: "dead player switches to spectator"
      pattern: "SpectatorMode"
    - from: "VictoryScreen.js"
      to: "Scoring.js"
      via: "display score from Scoring"
      pattern: "getTotalScore"
---

<objective>
Implement win/lose conditions, victory/game over screens, scoring, and restart functionality.

Purpose: The game needs clear victory and defeat conditions. Victory requires identifying the ghost and escaping; defeat occurs when all players die. The scoring system rewards evidence collection, correct identification, and successful escape.

Output:
- Win/lose condition checking
- Ghost type guess modal
- Exit door unlock mechanic
- Player death and spectator mode
- Victory and game over screens
- Scoring system
- Restart functionality
</objective>

<execution_context>
@C:/Users/user/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/user/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/05-evidence/05-RESEARCH.md
@.planning/phases/05-evidence/05-01-PLAN.md
@.planning/phases/04-ghost/04-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Win conditions and guess modal</name>
  <files>src/game/WinConditions.js, src/ui/GuessModal.js</files>
  <action>
Create WinConditions (src/game/WinConditions.js):

1. Constructor():
   - canGuess: false (requires 2+ evidence)
   - exitOpen: false
   - ghostIdentified: false
   - gamePhase: 'playing' | 'victory' | 'defeat'

2. checkCanGuess(collectedEvidence):
   - canGuess = collectedEvidence.length >= 2
   - return canGuess

3. makeGuess(selectedType, actualType):
   - If !canGuess: return { valid: false, reason: 'No evidence' }
   - ghostIdentified = (selectedType === actualType)
   - Return { valid: true, correct: ghostIdentified }

4. openExit():
   - If ghostIdentified: exitOpen = true
   - return exitOpen

5. checkVictory(players):
   - Return players.every(p => p.hasEscaped)

6. checkDefeat(players):
   - Return players.every(p => p.isDead)

7. setGamePhase(phase): gamePhase = phase

Create GuessModal (src/ui/GuessModal.js):

1. Constructor():
   - modal element
   - isOpen: false

2. open(possibleGhosts, canGuess):
   - isOpen = true
   - Render 6 ghost type buttons
   - Disable if !canGuess
   - Filter by possibleGhosts from JournalManager

3. onSelect(callback):
   - When player selects ghost type
   - callback(selectedType)
   - close()

4. close():
   - isOpen = false
   - Hide modal

Integration: G key toggles modal, ESC closes.

Reference patterns from RESEARCH.md (Win Condition Pattern, Ghost Type Selection).
  </action>
  <verify>
G key opens guess modal
Modal disabled without 2+ evidence
Selecting ghost type returns result
Correct ID opens exit door
  </verify>
  <done>
WinConditions class with guess validation
GuessModal class with ghost selection
Exit unlock on correct guess
  </done>
</task>

<task type="auto">
  <name>Task 2: Exit door and escape mechanic</name>
  <files>src/game/ExitDoor.js</files>
  <action>
Create ExitDoor (src/game/ExitDoor.js):

1. Constructor(scene):
   - position: Vector3 (spawns at exit location)
   - isOpen: false
   - doorMesh: Three.js mesh
   - interactionRange: 2 meters

2. unlock():
   - isOpen = true
   - Play unlock sound (procedural)
   - Animate door opening

3. checkEscape(player):
   - If isOpen && player in range:
     - player.hasEscaped = true
     - player.hide()
     - Return true
   - Return false

4. getPosition(): return position

5. renderOpenState():
   - Visual indicator (green light when open)
   - "Press E to escape" prompt

Placement: Exit door appears in designated spawn/foyer area when exit opens.

Reference patterns from RESEARCH.md (Exit Door Unlock, Team Escape).
  </action>
  <verify>
Exit door shows locked initially
Door unlocks on correct guess
Green indicator shows open state
Player can escape when in range
  </verify>
  <done>
ExitDoor class with unlock state
Escape detection
Visual open/locked indicators
  </done>
</task>

<task type="auto">
  <name>Task 3: Player death and spectator mode</name>
  <files>src/game/DeathHandler.js, src/game/SpectatorMode.js</files>
  <action>
Create DeathHandler (src/game/DeathHandler.js):

1. Constructor():
   - deathCause: 'caught' | 'sanity'
   - deathTime: timestamp

2. onCatch(player):
   - Trigger death sequence
   - play death scream (procedural)
   - screen flash red
   - Set player.isDead = true

3. onSanityDeath(player):
   - If player.sanity <= 0:
     - Trigger sanity death
     - hallucinations max
     - Set player.isDead = true

4. notifyServer(): Sync death state to server

Create SpectatorMode (src/game/SpectatorMode.js):

1. Constructor():
   - isSpectating: false
   - spectateTarget: player to follow

2. startSpectating(player):
   - isSpectating = true
   - Attach camera to nearest alive player
   - OR free-fly camera mode

3. cycleTarget():
   - Cycle through alive players
   - Switch spectate view

4. update():
   - Camera follows target smoothly
   - Show "Spectating: Player X" UI

5. canSpectate(): return player.isDead && !player.hasEscaped

Integration: Dead players automatically enter spectator mode.

Reference patterns from RESEARCH.md (Player Death Mechanic, Spectator Mode).
  </action>
  <verify>
Ghost catch triggers death sequence
Sanity 0 triggers death
Dead player camera switches to spectator
Can cycle between alive players
  </verify>
  <done>
DeathHandler class with death triggers
SpectatorMode class with camera handling
Auto-spectate on death
  </done>
</task>

<task type="auto">
  <name>Task 4: Victory and game over screens</name>
  <files>src/ui/VictoryScreen.js, src/ui/GameOverScreen.js</files>
  <action>
Create VictoryScreen (src/ui/VictoryScreen.js):

1. Constructor():
   - overlay element
   - isVisible: false

2. show(gameStats):
   - isVisible = true
   - Render stats:
     * Evidence found: X/6
     * Ghost identified: [type]
     * Time: MM:SS
     * Players escaped: X/4
     * Score: XXXX
   - Show "Victory!" text
   - Show "Press R to Restart" button

3. renderStats(stats):
   - Clear previous stats
   - Populate stat fields

4. hide(): isVisible = false

Create GameOverScreen (src/ui/GameOverScreen.js):

1. Constructor():
   - overlay element
   - isVisible: false

2. show(defeatReason):
   - isVisible = true
   - Render:
     * "Game Over" text
     * Reason: "All players caught" or "Everyone escaped"
     * Ghost type revealed
     * "Press R to Restart" button

3. hide(): isVisible = false

Styling: Dark overlay, centered content, semi-transparent background.

Integration: Trigger on win/lose conditions met.
  </action>
  <verify>
Victory shows on all players escaped
Game Over shows on all dead
Stats display correctly
Restart button works
  </verify>
  <done>
VictoryScreen class with stats display
GameOverScreen class with restart option
Proper trigger conditions
  </done>
</task>

<task type="auto">
  <name>Task 5: Scoring system</name>
  <files>src/game/Scoring.js</files>
  <action>
Create Scoring (src/game/Scoring.js):

1. Constructor():
   - evidencePoints: 100 per evidence
   - identificationPoints: 500
   - escapePoints: 200 per player
   - timeBonusPoints: 300 (under 5 min)
   - perfectBonus: 100 (all evidence)
   - totalScore: 0

2. addEvidence(count):
   - evidenceScore = count * evidencePoints
   - If count == 6: evidenceScore += perfectBonus
   - totalScore += evidenceScore

3. addIdentification(correct):
   - If correct: totalScore += identificationPoints

4. addEscape(count):
   - escapeScore = count * escapePoints
   - totalScore += escapeScore

5. addTimeBonus(elapsedMs):
   - If elapsedMs < 300000 (5 min): totalScore += timeBonusPoints

6. getTotalScore(): return totalScore

7. getBreakdown(): return { evidence, identification, escape, time, total }

Scoring display: Show in victory screen.

Reference patterns from RESEARCH.md (Scoring System).
  </action>
  <verify>
Evidence found adds 100 points
Correct ID adds 500 points
Each escape adds 200 points
Time under 5 min adds 300 bonus
  </verify>
  <done>
Scoring class with point calculation
Breakdown for display
Time-based bonus
  </done>
</task>

<task type="auto">
  <name>Task 6: Restart functionality</name>
  <files>src/game/GameManager.js</files>
  <action>
Add restart to GameManager (or create new):

1. restartGame():
   - Reset all player states (sanity 100, alive, not escaped)
   - Reset ghost to new random type
   - Regenerate map (optional)
   - Reset evidence found
   - Reset win/lose conditions
   - Reset scoring
   - Reset timers

2. resetPlayer(player):
   - isDead = false
   - hasEscaped = false
   - sanity = 100
   - battery = 100
   - position = spawn

3. resetGhost():
   - Pick new random ghost type
   - Reset state machine to IDLE
   - Reset position to ghost room

4. trigger(): R key or button click

Integration: R key during victory/game over triggers restart.

Reference patterns from RESEARCH.md (Restart Option).
  </action>
  <verify>
R key triggers restart during end screens
All game state resets properly
New game begins fresh
  </verify>
  <done>
Restart function with full reset
R key binding for restart
Clean game state initialization
  </done>
</task>

</tasks>

<verification>
1. Collect 2+ evidence, press G - guess modal opens
2. Select correct ghost type - exit door unlocks
3. All players reach exit - victory screen shows
4. Ghost catches player - death triggers
5. Dead player enters spectator mode
6. All dead - game over screen shows
7. Score calculated correctly
8. R key restarts game
</verification>

<success_criteria>
- [ ] Guess modal requires 2+ evidence
- [ ] Correct guess unlocks exit
- [ ] All escaped = victory
- [ ] All dead = game over
- [ ] Spectator mode works
- [ ] Scoring accurate
- [ ] Restart works
</success_criteria>

<output>
After completion, create `.planning/phases/05-evidence/05-02-SUMMARY.md`
</output>