---
phase: 02-environment
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/environment/map-generator.js
  - src/environment/room.js
  - src/environment/texture-generator.js
  - src/environment/lighting.js
  - src/game/scene.js
autonomous: true
requirements:
  - MAP-01
  - MAP-02
  - MAP-03
  - MAP-08
  - MAP-09
  - MAP-10
must_haves:
  truths:
    - "Player loads into a procedurally generated Victorian house with attic, ground floor, and basement"
    - "Walls display aged plaster texture (canvas-generated, no external images)"
    - "Floors display wood plank texture (canvas-generated, no external images)"
    - "Scene has atmospheric fog (FogExp2, density 0.035, dark blue-black)"
    - "Ambient light provides near-black blue tint illumination"
    - "Moonlight streams through windows as SpotLight with volumetric cone effect"
  artifacts:
    - path: "src/environment/map-generator.js"
      provides: "Procedural Victorian house generation with 3 floors"
      exports: ["MapGenerator", "generate()"]
      min_lines: 150
    - path: "src/environment/room.js"
      provides: "Room class with dimensions, doors, exits"
      exports: ["Room", "class"]
      min_lines: 50
    - path: "src/environment/texture-generator.js"
      provides: "Canvas-generated wall/floor textures"
      exports: ["generateWallTexture()", "generateFloorTexture()"]
      min_lines: 80
    - path: "src/environment/lighting.js"
      provides: "Atmospheric lighting setup"
      exports: ["setupAtmosphere()", "createMoonlight()", "Candle", "class"]
      min_lines: 60
  key_links:
    - from: "map-generator.js"
      to: "scene.js"
      via: "addToScene() call"
      pattern: "scene\\.add.*"
    - from: "texture-generator.js"
      to: "map-generator.js"
      via: "texture parameter"
      pattern: "texture.*=.*generate"
    - from: "lighting.js"
      to: "scene.js"
      via: "scene.add(light)"
      pattern: "scene\\.add.*light"
---

<objective>
Build the procedural Victorian haunted house map with atmospheric lighting.

Purpose: Create the core haunted house environment that players explore. All textures and geometry must be procedurally generated (zero external assets).

Output: 
- Procedural map generator with room-based generation across 3 floors (attic, ground, basement)
- Canvas-generated textures for walls (plaster with cracks/stains) and floors (wood planks)
- Atmospheric fog and lighting (ambient, moonlight, candles)
</objective>

<execution_context>
@C:/Users/user/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/user/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/02-environment/02-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Procedural map generator with room-based generation</name>
  <files>src/environment/map-generator.js, src/environment/room.js</files>
  <action>
Create MapGenerator class that:
1. Uses seeded PRNG (alea) for deterministic generation from seed string
2. Generates 3 floors (attic=2, ground=0, basement=-1 in Y axis)
3. Each floor has 4-7 rooms randomly placed with collision resolution
4. Rooms connect via doors (marked as door type in grid)
5. Central staircase connects all floors at fixed position
6. Returns room layout data usable by renderer

Create Room class that:
- Stores room id, position (x,z), dimensions (width, depth), floor level
- Contains doors[] array with positions and rotations
- Provides getBoundingBox() for collision detection

Use grid-based approach from RESEARCH.md Pattern 1. Rooms stored in array, no need for complex BSP.
  </action>
  <verify>
npm run dev starts server without console errors
Check generated map has 3 floors with 4-7 rooms each
Rooms are separated (no overlapping bounding boxes)
Staircase connects all floors
  </verify>
  <done>
MapGenerator.generate(seed, floors=3) returns object with floors array
Each floor contains rooms array with position/dimensions
Staircase exists at center connecting all floors
</done>
</task>

<task type="auto">
  <name>Task 2: Canvas-based procedural textures</name>
  <files>src/environment/texture-generator.js</files>
  <action>
Create TextureGenerator class with static methods:

1. generateWallTexture(width=512, height=512) returns CanvasTexture:
   - Base color: aged plaster (#d4d0c8 lerped to #8a8780 at 0.3)
   - Add noise via ImageData manipulation (±30 RGB variance)
   - Draw 3-5 cracks using bezier curves
   - Draw 2-3 water stain gradients
   - Return as THREE.CanvasTexture with RepeatWrapping

2. generateFloorTexture(width=512, height=512, seed) returns CanvasTexture:
   - Base dark wood (#2a1f1a)
   - Draw vertical planks (64px wide, seeded variation per plank)
   - Add wood grain lines per plank (seeded noise)
   - Return as THREE.CanvasTexture with RepeatWrapping

3. generateDoorTexture(width=256, height=256) returns CanvasTexture:
   - Dark wood base with panels
   - Seeded variation

Use alea PRNG for seeded randomness. All textures use RepeatWrapping with repeat.set(2,2) for walls and (4,4) for floors.
  </action>
  <verify>
Wall texture shows plaster with visible cracks/stains at close range
Floor texture shows wood planks with grain pattern
Door texture shows paneled wood door
Textures tile seamlessly (no seams visible)
  </verify>
  <done>
Textures are THREE.CanvasTexture objects (not external images)
Wall texture generates with cracks and stains
Floor texture shows wood grain pattern
</done>
</task>

<task type="auto">
  <name>Task 3: Atmospheric lighting with fog, ambient, moonlight, candles</name>
  <files>src/environment/lighting.js</files>
  <action>
Create lighting module with:

1. setupAtmosphere(scene) - Sets fog and ambient:
   - scene.fog = new THREE.FogExp2(0x0a0a12, 0.035)
   - scene.background = new THREE.Color(0x0a0a12)
   - scene.ambient = new THREE.AmbientLight(0x1a1a2e, 0.15)
   - Add ambient to scene

2. createMoonlight(position, target) returns group:
   - Creates SpotLight(color=0xc7ddff, intensity=10, distance=50, angle=PI/6)
   - Creates transparent cone mesh for volumetric effect (opacity=0.08, additive blending)
   - Returns group containing both

3. Candle class (from RESEARCH.md Pattern 5):
   - Constructor(position): creates candle mesh + point light
   - update(time, deltaTime): flickers intensity using Math.sin waves
   - Property: baseIntensity=2, flickerSpeed=15, flickerAmount=0.3

Place moonlight only at window positions (north-facing exterior walls).
Randomly place 3-5 candles per floor using PRNG.
  </verify>
Fog renders with exponential falloff (objects fade, not clip)
Ambient provides minimal blue-tinted visibility
Moonlight creates visible cone effect through windows
Candles flicker continuously with varying intensity
  </verify>
  <done>
scene.fog is FogExp2 with density 0.035
AmbientLight has color 0x1a1a2e, intensity 0.15
Moonlight SpotLight + cone visible at windows
Candle lights flicker with no static values
</done>
</task>

</tasks>

<verification>
1. Run `npm run dev` - server starts without errors
2. Open browser - see Victorian house structure (walls, floors, doors)
3. Check textures: walls show plaster with cracks/stains, floors show wood grain
4. Check fog: objects fade naturally with distance
5. Check lighting: ambient very dim, moonlight visible through windows, candles flicker
</verification>

<success_criteria>
- [ ] MapGenerator generates 3 floors with 4-7 rooms each
- [ ] Procedural textures (wall, floor, door) via Canvas API
- [ ] FogExp2 atmosphere with density 0.035
- [ ] Ambient light (near-black blue tint)
- [ ] Moonlight SpotLight with volumetric cone through windows
- [ ] Candle class with flickering light
</success_criteria>

<output>
After completion, create `.planning/phases/02-environment/02-01-SUMMARY.md`
</output>